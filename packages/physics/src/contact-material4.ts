export interface ContactMaterial4 {
  /** Isotropic Coulomb coefficient. Default 0.5. */
  readonly friction?: number;
  /** Newton restitution coefficient. Default 0. */
  readonly restitution?: number;
}
